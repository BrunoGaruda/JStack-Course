console.log(`essa é a primeira linha
essa é segunda linha
`)

const valor = Math.random() * 100
const name = 'Bruno'

console.log(
  `Seja bem-vindo ${name}, esse é seu saldo de sua conta? R$ ${valor}, ${
    valor > 50 ? 'sim' : 'não'
  }`
)
